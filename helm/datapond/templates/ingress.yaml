{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: datapond-ingress
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: datapond
    app.kubernetes.io/instance: {{ .Release.Name }}
  annotations:
    {{- range $key, $value := .Values.ingress.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  {{- $ingressHost := .Values.ingress.domain | default .Values.global.domain }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
  - hosts:
    - {{ $ingressHost }}
    secretName: {{ .Values.ingress.tls.secretName }}
  {{- end }}
  rules:
  - host: {{ $ingressHost }}
    http:
      paths:
      # Frontend
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.frontend.name }}
            port:
              number: {{ .Values.frontend.service.port }}

      # Backend API
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.backend.name }}
            port:
              number: {{ .Values.backend.service.port }}

      {{- if (include "datapond.addonOn" (dict "root" $ "component" "jupyter")) }}
      # JupyterLab
      - path: /jupyter
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.jupyter.name }}
            port:
              number: {{ .Values.jupyter.service.port }}
      {{- end }}

      {{- if (include "datapond.addonOn" (dict "root" $ "component" "mlflow")) }}
      # MLflow
      - path: /mlflow
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.mlflow.name }}
            port:
              number: {{ .Values.mlflow.service.port }}
      {{- end }}

      {{- if (include "datapond.addonOn" (dict "root" $ "component" "airflow")) }}
      # Airflow
      - path: /airflow
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.airflow.name }}
            port:
              number: {{ .Values.airflow.webserver.service.port }}
      {{- end }}

      {{- if (include "datapond.addonOn" (dict "root" $ "component" "spark")) }}
      # Spark UI
      - path: /spark
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.spark.name }}-master
            port:
              number: {{ .Values.spark.master.service.webPort }}
      {{- end }}

      {{- if .Values.minio.enabled }}
      # MinIO Console (Web UI)
      - path: /minio
        pathType: Prefix
        backend:
          service:
            name: minio
            port:
              number: 9001
      {{- end }}

      {{- if (include "datapond.addonOn" (dict "root" $ "component" "trino")) }}
      # Trino (SQL Analytics)
      - path: /trino
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.trino.name }}
            port:
              number: {{ .Values.trino.service.port }}
      {{- end }}

      {{- if (include "datapond.addonOn" (dict "root" $ "component" "risingwave")) }}
      # RisingWave Dashboard
      - path: /risingwave
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.risingwave.name }}-meta
            port:
              number: {{ .Values.risingwave.meta.service.dashboardPort }}
      {{- end }}

      {{- if (include "datapond.addonOn" (dict "root" $ "component" "openmetadata")) }}
      # OpenMetadata
      - path: /openmetadata
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.openmetadata.name }}-server
            port:
              number: {{ .Values.openmetadata.server.service.port }}
      {{- end }}
{{- end }}
