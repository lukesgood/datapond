fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0
httpx==0.26.0
python-multipart==0.0.6
psycopg2-binary==2.9.9
sqlalchemy==2.0.25
redis==5.0.1
kubernetes==29.0.0
trino==0.328.0
sqlglot==26.33.0  # RLS Layer1: SQL 파싱·행필터/마스킹 재작성 (docs/RLS_DESIGN.md)
# mlflow → mlflow-skinny: 백엔드는 MlflowClient/entities/exceptions만 사용(REST 트래킹 클라이언트).
# full mlflow는 pyarrow<16을 강제해 pyiceberg(pyarrow>=16)와 충돌하나, skinny는 pyarrow 비의존이라 충돌 0.
mlflow-skinny==2.10.2
# Data connector dependencies
asyncpg==0.29.0
pymysql==1.1.0
boto3==1.34.0
pandas==2.1.4
# Iceberg 직접 쓰기 (PyIceberg → Polaris REST + SeaweedFS S3). 스파이크 검증: 0.11.1
# s3fs 미포함: PyIceberg는 PyArrowFileIO(s3.* 프로퍼티)로 S3에 직접 쓴다(pod에서 확인).
#   s3fs는 aiobotocore를 끌어와 boto3==1.34(botocore<1.35)와 충돌하므로 제외.
pyiceberg[pyarrow]==0.11.1
pyarrow>=16,<25
cryptography==41.0.7
jinja2==3.1.3
python-jose[cryptography]==3.3.0
bcrypt==4.0.1
anthropic==0.40.0
ldap3==2.9.1
webauthn>=2.0.0
